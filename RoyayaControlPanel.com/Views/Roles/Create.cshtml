@model Microsoft.AspNet.Identity.EntityFramework.IdentityRole

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Admin_Layout.cshtml";
}
<div>
    <h2 style="color: #8b224e">Enter Role's Information </h2>
    <br />

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            <hr />
            @Html.ValidationSummary(true)

            <div class="form-group" style="font-size:x-large ">
    @Html.LabelFor(model => model.Name, new { @class = "control-label col-md-2" })
                <div class="col-md-10" style="margin-top:1%">
                    @Html.EditorFor(model => model.Name)
                    @Html.ValidationMessageFor(model => model.Name)
                </div>
</div>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Create" style="color: #fff" class="btn btn-default" />

                </div>
            </div>
        </div>
    }
    @*<button type="button" class="btn btn-primary">Primary</button>*@
    <button class="btn btn-primary btn-default" onclick="location.href='@Url.Action("Index", "Roles")'">Back to List
    </button>

        @*<div>
                @Html.ActionLink("Back to List", "Index")
            </div>*@
</div>
